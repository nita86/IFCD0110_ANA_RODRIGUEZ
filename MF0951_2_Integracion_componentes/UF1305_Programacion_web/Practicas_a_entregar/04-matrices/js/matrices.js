//funciones JavaScript

//ejercicio1
function ejercicio1() {

}

//ejercico2
function ejercicio2() { 
    var matriz=new Array("Seat", "Audi", "Skoda");
    var textoIncluir=document.getElementById("text2").value;
    var cortar=textoIncluir.split(" ");//separamos cada palabra y las transformamos en valores individuales
    var primerEspacio=inicial.indexOf(" ");
    var primerValor=inicial.slice(0,primerEspacio);
    var ultimoEspacio=inicial.lastIndexOf(" ");
    var segundoValor=inicial.slice(primerEspacio+1, ultimoEspacio);
    var tercerValor=inicial.slice(ultimoEspacio+1);
    var incluir=matriz.push("")
}